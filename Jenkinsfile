node("worker1") {
    def name="Esteban"
    stage('Who am I?') {
        println "$name"
    }
    stage('Running Groovy') {
        numbers()
        numbers()
        println 'now this should run in worker1'
        }
}
